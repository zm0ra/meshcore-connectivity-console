services:
  init-db:
    build:
      context: .
    command: ["python", "-m", "meshcore_bot", "init-db", "--config", "/app/config/config.toml"]
    restart: "no"
    volumes:
      - ./config:/app/config
      - ./data:/app/data

  ensure-identity:
    build:
      context: .
    command: ["python", "-m", "meshcore_bot", "ensure-identity", "--config", "/app/config/config.toml"]
    restart: "no"
    depends_on:
      init-db:
        condition: service_completed_successfully
    volumes:
      - ./config:/app/config
      - ./data:/app/data

  bridge-gateway:
    build:
      context: .
    command: ["python", "-m", "meshcore_bot", "run-bridge-gateway", "--config", "/app/config/config.toml"]
    restart: unless-stopped
    depends_on:
      init-db:
        condition: service_completed_successfully
      ensure-identity:
        condition: service_completed_successfully
    volumes:
      - ./config:/app/config
      - ./data:/app/data

  neighbours-worker:
    build:
      context: .
    command: ["python", "-m", "meshcore_bot", "run-neighbours-worker", "--config", "/app/config/config.toml"]
    restart: unless-stopped
    depends_on:
      bridge-gateway:
        condition: service_started
      init-db:
        condition: service_completed_successfully
      ensure-identity:
        condition: service_completed_successfully
    volumes:
      - ./config:/app/config
      - ./data:/app/data

  web:
    build:
      context: .
    command: ["python", "-m", "meshcore_bot", "run-web", "--config", "/app/config/config.toml"]
    restart: unless-stopped
    depends_on:
      init-db:
        condition: service_completed_successfully
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
